# Core - PyTorch with ROCm support
# Install via: pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm6.1
torch==2.9.1+rocm7.2.1
torchvision==0.20.1+rocm7.2.1

# Simulation
genesis-world==1.2.3

# Robot Learning Framework
lerobot==0.6.0

# Model dependencies (SmolVLA)
transformers>=4.40.0
datasets>=2.14.0
huggingface_hub>=0.17.0
accelerate>=0.20.0
safetensors>=0.3.0

# Image/Video Processing
opencv-python-headless>=4.8.0
Pillow>=10.0.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# Numerical
numpy>=1.24.0
scipy>=1.10.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
requests>=2.31.0
websocket-client>=1.6.0

# JupyterLab API (for remote execution utilities)
jupyter-client>=8.3.0
jupyter-core>=5.3.0

# Browser automation (for remote execution utilities)
playwright>=1.40.0

# NOTE: MPLBACKEND=Agg must be set as an environment variable for headless
# rendering. It cannot be set in requirements.txt. Set it in your shell or
# Dockerfile, e.g.: export MPLBACKEND=Agg
